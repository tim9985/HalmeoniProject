# 🏗️ 시스템 아키텍처

할매 프로젝트의 전체 시스템 구조와 데이터 흐름을 설명합니다.

## 📊 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                         할머니 스마트폰                              │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │           React 웹 애플리케이션 (Client)                      │ │
│  │  ┌────────┐  ┌─────────┐  ┌─────────┐  ┌──────────┐      │ │
│  │  │ Header │  │  Menu   │  │  Bible  │  │   API    │      │ │
│  │  │        │  │ Buttons │  │  Verse  │  │  Service │      │ │
│  │  └────────┘  └─────────┘  └─────────┘  └──────────┘      │ │
│  └───────────────────────────────────────────────────────────┘ │
│                            ↕ HTTP/HTTPS                         │
└─────────────────────────────────────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│                   백엔드 서버 (Server)                             │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                    Express.js Server                      │ │
│  │  ┌────────┐  ┌──────────┐  ┌─────────┐                   │ │
│  │  │ Routes │→ │Controller│→ │ Service │                   │ │
│  │  └────────┘  └──────────┘  └─────────┘                   │ │
│  └───────────────────────────────────────────────────────────┘ │
│                            ↕ API Calls                          │
└─────────────────────────────────────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│                      외부 API 서비스                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │ 부산 BIS │  │ YouTube  │  │  기상청   │  │네이버지도 │      │
│  │  (버스)  │  │(메세지)  │  │  (날씨)  │  │(길찾기)  │      │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘      │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 데이터 흐름

### 1. 버스 도착 정보 조회

```
할머니 화면
    ↓ (앱 로드 / 1분마다 자동 갱신)
Client API Service (getChurchBus)
    ↓ GET /api/church-bus
Server Route → Controller → Bus Service
    ↓ API 요청
부산 버스정보시스템 (BIS API)
    ↓ XML 응답
Server (파싱 및 가공)
    ↓ JSON 응답
Client (화면 표시)
    ↓
할머니: "🚌 3006번: 5분 후 (2정거장 전)"
```

**실시간 갱신**: 60초마다 자동으로 버스 정보 업데이트

---

### 2. 주일 메세지 영상 보기

```
할머니: "주일 1부 메세지" 버튼 클릭
    ↓
Client API Service (getLatestVideo)
    ↓ GET /api/latest-video?part=1
Server Route → Controller → YouTube Service
    ↓ API 요청
YouTube Data API v3
    ↓ 최신 15개 영상 조회
Server (필터링 및 선택)
    - "주일 1부" 키워드 포함
    - "전체 예배" 제외
    - 가장 최신 영상 선택
    ↓ JSON 응답 (URL)
Client (YouTube로 리다이렉트)
    ↓
할머니: YouTube 앱/웹에서 영상 시청
```

**스마트 필터링**: 전체 예배 영상 제외, 메세지만 추출

---

### 3. 날씨 정보 조회

```
할머니 화면 (앱 로드)
    ↓
Client API Service (getCurrentWeather)
    ↓ GET /api/weather
Server Route → Controller → Weather Service
    ↓ API 요청 (격자 좌표 nx=98, ny=76)
기상청 단기예보 API
    ↓ JSON 응답 (기온, 하늘상태, 강수형태)
Server (데이터 파싱 및 가공)
    - 기온 → 정수 변환
    - 하늘상태 → "맑음/구름많음/흐림"
    - 강수형태 → "비/눈/소나기"
    - 아이콘 선택 → "☀️/⛅/☁️/🌧️/❄️"
    ↓ JSON 응답
Client (헤더에 표시)
    ↓
할머니: "☀️ 오늘 15도 맑음"
```

**갱신 주기**: 30분마다 자동 업데이트

---

### 4. 성경 말씀 표시

```
할머니 화면 (앱 로드)
    ↓
Client API Service (getDailyVerse)
    ↓ GET /api/daily-verse
Server Route → Controller → Bible Service
    ↓ (외부 API 없음, 내장 데이터)
Server (날짜 기반 계산)
    - 오늘 날짜 → 연중 일수 계산
    - 일수 % 말씀 개수 = 인덱스
    - 해당 말씀 반환
    ↓ JSON 응답
Client (하단에 표시)
    ↓
할머니: "📖 오늘의 말씀
        하나님이 세상을 이처럼 사랑하사...
        - 요한복음 3:16"
```

**특징**: 외부 API 없이 작동, 매일 다른 말씀

---

### 5. 길찾기 (네이버 지도)

```
할머니: "교회 가기" 버튼 클릭
    ↓
Client Navigation Utility (openNaverMap)
    ↓ (디바이스 감지)
모바일 여부 확인
    ↓
[모바일인 경우]
    1. 네이버 지도 앱 URL 스킴 호출
       nmap://route/public?dlat=35.0768&dlng=129.0718...
    2. 앱 미설치 시 1.5초 후 웹 버전 fallback
    ↓
[PC인 경우]
    네이버 지도 웹 버전 새 탭 열기
    ↓
할머니: 네이버 지도에서 길찾기 결과 확인
```

**스마트 연동**: 앱 → 웹 자동 전환

---

## 🗂️ 코드 구조

### 클라이언트 (React + TypeScript)

```
client/src/
├── App.tsx                 # 메인 앱 (조합 로직)
│   └── useState/useEffect로 상태 관리
│
├── components/             # UI 컴포넌트 (View)
│   ├── Header.tsx         # 날씨, 이벤트 표시
│   ├── MenuButton.tsx     # 재사용 버튼
│   └── BibleVerse.tsx     # 성경 말씀 표시
│
├── services/              # API 통신 레이어
│   └── api.ts             # axios 기반 API 호출
│
├── utils/                 # 유틸리티 함수
│   └── navigation.ts      # 네비게이션 로직
│
└── types/                 # TypeScript 타입
    └── index.ts           # 인터페이스 정의
```

### 서버 (Node.js + Express)

```
server/src/
├── server.js              # 서버 진입점 (Express 설정)
│   ├── CORS 설정
│   ├── 라우트 연결
│   └── 에러 핸들링
│
├── routes/                # 라우트 정의
│   └── api.js             # API 엔드포인트 매핑
│
├── controllers/           # 요청/응답 처리
│   └── apiController.js   # 비즈니스 로직 호출
│
└── services/              # 비즈니스 로직 (핵심)
    ├── busService.js      # 버스 API 통신 + 파싱
    ├── youtubeService.js  # YouTube API 통신 + 필터링
    ├── weatherService.js  # 날씨 API 통신 + 변환
    └── bibleService.js    # 성경 말씀 데이터 관리
```

---

## 🔐 보안 고려사항

### 1. API 키 관리
- ✅ `.env` 파일로 분리
- ✅ `.gitignore`에 포함
- ✅ 서버 환경에서만 사용 (클라이언트 노출 방지)

### 2. CORS 설정
```javascript
// server/src/server.js
const corsOptions = {
  origin: [
    'http://localhost:5173',        // 개발
    'https://your-app.vercel.app'   // 프로덕션
  ]
};
```

### 3. 환경 변수
- **서버**: `.env` (BUS_API_KEY, YOUTUBE_API_KEY 등)
- **클라이언트**: `.env` (VITE_API_URL만)

---

## ⚡ 성능 최적화

### 1. 캐싱 전략
- **버스 정보**: 60초 간격 폴링 (불필요한 요청 방지)
- **날씨 정보**: 30분 간격 폴링
- **성경 말씀**: 앱 로드 시 1회만 조회

### 2. 에러 처리
```javascript
// 모든 API 호출에 try-catch
try {
  const data = await apiCall();
} catch (error) {
  console.error(error);
  return fallback; // 사용자에게 오류 메시지 대신 기본값
}
```

### 3. 타임아웃 설정
```javascript
// 서버 API 호출 시 타임아웃 (5초)
axios.get(url, { timeout: 5000 });
```

---

## 📱 반응형 디자인

### 모바일 우선 설계
- **버튼 크기**: 최소 44x44px (터치 친화적)
- **글씨 크기**: 24-26px (가독성)
- **간격**: 충분한 여백 (실수 방지)
- **스크롤**: 최소화 (한 화면에 모든 기능)

### 디바이스 호환성
- ✅ Android 7.0+
- ✅ iOS 12+
- ✅ Chrome, Safari, Samsung Internet

---

## 🔄 업데이트 프로세스

### 코드 업데이트
```bash
# 1. 로컬 개발
git pull
# 코드 수정
git add .
git commit -m "기능 개선"
git push

# 2. 자동 배포 (Vercel/Railway)
# → Git push 시 자동으로 재배포
```

### 긴급 수정
1. **버그 발견** → 로컬에서 수정
2. **테스트** → 로컬 서버에서 확인
3. **배포** → Git push
4. **확인** → 프로덕션 환경 테스트

---

## 📊 모니터링 포인트

### 정기 점검 항목
- [ ] 버스 정보 정확성
- [ ] YouTube 영상 자동 로드
- [ ] 날씨 정보 갱신
- [ ] API 키 유효성
- [ ] 서버 응답 시간

### 로그 확인
```bash
# Railway/Render 대시보드에서
- 에러 로그
- API 호출 횟수
- 응답 시간
```

---

## 🎯 핵심 설계 원칙

1. **단순성**: 복잡한 기능 최소화
2. **직관성**: 아이콘과 색상으로 구분
3. **안정성**: 에러 발생 시에도 작동
4. **접근성**: 큰 버튼, 큰 글씨
5. **반응성**: 빠른 로딩, 실시간 업데이트

---

## 🔮 향후 확장 가능성

### 추가 기능 아이디어
1. **알림 기능**: 주일/화요일 리마인더
2. **메모 기능**: 간단한 할 일 목록
3. **긴급 연락**: 가족 연락처 바로가기
4. **건강 정보**: 복약 리마인더
5. **쇼핑 도우미**: 쿠팡 가이드

### 기술 스택 확장
- **PWA**: 오프라인 지원
- **Push Notification**: 알림 기능
- **Speech Recognition**: 음성 명령
- **다크 모드**: 눈 편한 테마

---

**설계 철학**: "할머니가 쉽고 편하게" ❤️
